import { Hoop } from "@/components/Hoop";

// Doodle Jump rule: the game is the screen. One readout bar, one status
// line, zero decoration outside the canvas — the hand-touched details
// (rafter pennants, parquet seams, the creature) all live inside the
// world. See components/Hoop.tsx.

export default function Home() {
  return <Hoop />;
}
